import db from "@/lib/helper/prismaClient";
// import { fetchAllFilters } from "@/data/inputs";

export default async function DbTesting() {
  // const filters = await fetchAllFilters();
  // console.log(filters);

  return <p>Testing the Drizzle Data Handling</p>;
}
